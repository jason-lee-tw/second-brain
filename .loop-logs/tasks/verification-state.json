{
  "rounds_completed": 1,
  "last_outcome": "pass",
  "notes": "All 6 ACs passed (JSONB codec fix). POST /query returns 200, no ValueError in logs, response contains all required fields. Container rebuilt via just up-all before runtime checks."
}
